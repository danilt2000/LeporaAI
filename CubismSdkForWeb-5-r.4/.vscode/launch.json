{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug Demo in Chrome",
      "type": "chrome",
      "request": "launch",
      "url": "http://localhost:5000",
      "webRoot": "${workspaceFolder}",
      "preLaunchTask": "npm start"
    }
  ]
}
